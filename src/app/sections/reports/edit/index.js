import angular from 'angular';
import core from '../../../core';
import ReportsEditCtrl from './reports-edit-controller.js';

export default angular.module('kagenSite.reportsEdit', [core])
  .controller(ReportsEditCtrl)
  .name;
